# API Contracts

REST-based APIs with JSON payloads.

Core endpoints:
- POST /complaints
- GET /complaints/{id}
- GET /analytics/heatmap
- POST /resolution

Authentication:
JWT-based with role-level access.
